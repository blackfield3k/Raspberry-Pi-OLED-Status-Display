Little Script for showing various informations on a SSD1306 OLED Display on an Raspberry Pi.

The config.py.example is pretty self explaining. Just a short reminder that you can't show more then four values on a 128x32 display.

Also don't forget to make the install.sh excutabable with chmod +x install.sh